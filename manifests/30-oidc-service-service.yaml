apiVersion: v1
kind: Service
metadata:
  name: oidc-service
  namespace: default
  labels:
    app: oidc-service
spec:
  selector:
    app: oidc-service
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
